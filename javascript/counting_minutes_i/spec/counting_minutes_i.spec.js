var countingMinutesI = require('../lib/counting_minutes_i.js');

describe('countingMinutesI', function() {
    it('is a function', function () {
        expect(typeof countingMinutesI).toBe('function');
    });
});